import {Box, VStack, Text, Input, Button} from "@chakra-ui/react";


const ChatArea = (props) => {

    return (
        <Box p={5} width="325px" bg="black"/*bg={colorMode === 'light' ? 'white' : 'gray.700'}*/>
        
        </Box>
    );

};

export default ChatArea;